# Copy to /etc/healer/healer.env and `chmod 600` it — it holds a secret.
# The setup wizard (healer-setup) writes this file for you; you should not normally need to edit
# it by hand. Loaded by systemd via EnvironmentFile= in healer.service.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
